pipeline {
    agent any
    stages {
    
        stage('Build') {
            steps {
                sh 'npm install'
                
            }
        }

        stage('Deploy - Production') {
            steps {
                sh '#'
            }
        }
    }
}
